{
  "name": "menubar",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bs-fetch": "0.1.1",
    "bs-json": "0.2.1",
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "reason-scripts": "0.6.6"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "prepare": "npm link bs-platform",
    "start-both": "concurrently 'yarn start' 'cd .. && yarn menubar'"
  },
  "devDependencies": {
    "bs-jest": "^0.1.0",
    "concurrently": "^3.5.0",
    "reason-react": "^0.2.3"
  }
}
